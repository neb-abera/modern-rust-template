# Keeps the crate dependencies, the SHA-pinned GitHub Actions and the
# Docker base image current. Dependabot PRs update the pinned SHA/digest and
# the version comment together, so supply-chain pinning and staying on the
# latest versions are compatible. Patch and minor updates are grouped per
# ecosystem into one PR; majors arrive alone and wait for a human. The
# five-day cooldown holds brand-new releases back long enough for a
# compromised or broken one to be yanked before it is ever proposed here.
version: 2
updates:
  - package-ecosystem: cargo
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 5
    groups:
      cargo-non-major:
        applies-to: version-updates
        update-types: [minor, patch]

  - package-ecosystem: cargo
    directory: /fuzz
    schedule:
      interval: weekly
    cooldown:
      default-days: 5
    groups:
      fuzz-non-major:
        applies-to: version-updates
        update-types: [minor, patch]

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 5
    groups:
      actions-non-major:
        applies-to: version-updates
        update-types: [minor, patch]

  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 5
